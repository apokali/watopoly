#ifndef UTIL_H
#define UTIL_H

#include <iostream>
#include <string>

// prints a newline at the end if newline == true
void printLine( std::ostream &out, std::string s, int num, bool newline);

#endif
